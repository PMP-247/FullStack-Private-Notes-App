{
  "name": "private-notes-fullstack",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build-frontend",
    "build-frontend": "cd personal-notes-app && npm install && npm run build",
    "install-server": "cd server && npm install"
  }
}